/**
 * Comes from JS Primitive types: number, boolean, string, NaN, undefined, null (these comes from JavaScript)
 * New Special Primitive types: never, unknown, void (new primitive types from TypeScript)
 


 */


let userName: string = "Sajib"; //explicitly telling that it is a string

// userName = 123; //it will give error before running the code as we can not assign a number inside a string or vice versa

let userId = 123; //implicitly or automatically infering which type

// userId = "id123"; // can not do it as it is number type

let y; //implicitly it will make it any

let x = undefined; // as implicitly it will make it any that is storing undefined.

let z: undefined = undefined; // now I am telling, it is an undefined type. 

//when I don't defined anything or assign any value into a variable then the type is any.



